# Fair AI Observatory — Python dependencies
# pandas<3.0 is pinned project-wide for Fairlearn 0.14.0 compatibility.
# Do not bump pandas without checking Fairlearn's release notes first.

pandas<3.0
numpy>=1.24
scikit-learn>=1.3
xgboost>=2.0
fairlearn==0.14.0
great-expectations>=0.18
dbt-core>=1.7
apache-airflow>=2.8
sqlalchemy>=2.0
psycopg2-binary
python-dotenv
pyyaml
